<t-message id="t-message" />
<view class="container">
  <view class="prompt">
    <view>请在详细填写问题描述、并尽可能上传故障照片和视频,方便工程师远程诊断并快速解决故障。</view>
  </view>
  <view class="content">
    <t-input style="{{nameBorderStyle}}" value="{{inputName}}" class="required" bindchange = "inputName" label="姓名" placeholder="请输入姓名"></t-input>
    <t-input style="{{phoneBorderStyle}}" value="{{inputPhone}}" class="required" bindchange = "inputPhone" label="联系方式" placeholder="请输入联系方式"></t-input>
    <t-input style="{{provinceBorderStyle}}" value="{{inputProvince}}" class="required" bindchange = "inputProvince" label="省份" placeholder="请输入省份"></t-input>
    <t-input style="{{countyBorderStyle}}" value="{{inputCounty}}" class="required" bindchange = "inputCounty" label="市县" placeholder="请输入市县"></t-input>
    <t-input style="{{addressBorderStyle}}" value="{{inputaddress}}" class="required" bindchange = "inputaddress" label="详细地址" placeholder="请输入详情地址"></t-input>
    <view class="con_category">
      <view class="category">问题类别</view>
      <t-radio-group class="radio" defaultValue="radio1" disabled="{{false}}">
        <t-radio class="radio_" value="radio1" label="售前咨询" />
        <t-radio class="radio_" value="radio2" label="售后报修" />
        <t-radio class="radio_" value="radio3" label="投诉建议" />
      </t-radio-group>
    </view>
    <view class="describe">问题描述</view>
    <view class="con_textarea">
      <t-textarea style="{{textareaBorderStyle}}" value="{{inputTextarea}}" bindchange = "inputTextarea" t-class="external-class" placeholder="请输入问题描述" maxlength="200" disableDefaultPadding="{{true}}" indicator />
    </view>
    <view class="con_billcode">
      <text class="billcode">选择订单</text>
      <view class="example-search">
        <t-search model:value="{{value}}" placeholder="搜索我的订单" />
      </view>
    </view>
    <view class="con_update">
      <text class="up_name">上传图片</text>
      <view class="wrapper">
        <t-upload disabled media-type="{{['video','image']}}" files="{{originFiles}}" gridConfig="{{gridConfig}}" bind:success="handleSuccess" bind:remove="handleRemove" bind:click="handleClick" bind:sort-end="handleSortEnd" />
      </view>
    </view>
  </view>
  <view class="submit">
    <t-button t-class="external-class" size="large" theme="primary" variant="outline" bindtap="handleTap">提交</t-button>
  </view>
</view>